---
- name: "Copy bashrc"
  copy:
    src: "{{ role_path }}/files/bash/bashrc"
    dest: "{{ ansible_env.HOME }}/.bashrc"

- name: "Install packages for env - Rust、Go"
  pacman:
    name:
      - rust
      - go
    state: latest
  become: true
  become_method: sudo