---
- name: "Sarch"
  hosts: localhost
  connection: local
  tasks:
    - name: "Demo Debug"
      debug: 
        msg: "{{ ansible_env.HOME }}"
  roles:
    - role: "roles/configure-base-env"
    - role: "roles/configure-desktop"
